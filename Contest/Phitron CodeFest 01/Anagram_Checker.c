#include<stdio.h>
#include<string.h>

int main() 
{
    char str1[100], str2[100];
    scanf("%s %s", str1, str2);

    int str1_len = strlen(str1);
    int str2_len = strlen(str2);

    for(int i = 0; i < str1_len; i++)

    return 0;
}